suite = "core"
n = 1000
seed = 42
adapters = ["siderust", "astropy", "libnova", "anise"]
siderust_profiles = ["iau2006a"]
no_build = false

[performance]
enabled = true
rounds = 10

[horizons]
use_cache = true
allow_network = true

[output]
dir = "results"
publish_latest = true
